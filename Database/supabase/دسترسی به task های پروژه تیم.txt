create policy "Team members can access tasks"
on tasks
for all
using (
  project_id in (
    select p.id from projects p
    join user_profiles up on up.team_id = p.team_access_id
    where up.id = auth.uid()
  )
);
