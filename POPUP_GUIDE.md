# راهنمای تغییرات پاپ‌آپ‌ها و مدیریت تیم (Teams Popups Guide)

این مستند برای توضیح تغییرات انجام شده روی پاپ‌آپ‌های صفحه مدیریت تیم (`Teams.aspx`) و نحوه کار با آن‌ها تهیه شده است.

## ۱. فایل `Teams.aspx` (فرانت‌اند)

در این فایل ساختار HTML پاپ‌آپ‌ها و کدهای جاوااسکریپت مربوطه قرار دارد.

### پاپ‌آپ ویرایش تیم (`editTeamModal`)
تغییرات اصلی در این بخش شامل اضافه شدن قابلیت مدیریت اعضا در هنگام ویرایش است.

*   **ساختار HTML:**
    *   بخش `members-section` اضافه شد که لیست اعضای فعلی را نشان می‌دهد.
    *   دکمه `+ Add Member` (`addEditMemberRow`) برای افزودن ردیف جدید در پاپ‌آپ ویرایش.
    *   دکمه ذخیره (`btnSaveEdit`) که تابع جاوااسکریپت `prepareEditTeamData` را قبل از ارسال فرم صدا می‌زند.

*   **جاوااسکریپت:**
    *   `showEditTeamModal()`: علاوه بر نمایش پاپ‌آپ، تابع `populateEditMembers()` را صدا می‌زند.
    *   `populateEditMembers()`: اطلاعات اعضای فعلی تیم را از `HiddenField` (`hfEditTeamMembersInitial`) می‌خواند و در پاپ‌آپ نمایش می‌دهد.
    *   `addEditMemberRow(username, role)`: یک ردیف جدید شامل اینپوت جستجو و دراپ‌داون نقش (Member/Admin) ایجاد می‌کند.
    *   `prepareEditTeamData()`: تمام اعضای موجود در لیست ویرایش را جمع‌آوری کرده و به صورت JSON در `hfEditTeamMembers` قرار می‌دهد تا سمت سرور قابل دسترسی باشد.

### پاپ‌آپ ساخت تیم (`createTeamModal`)
*   مشابه پاپ‌آپ ویرایش، امکان انتخاب نقش (Role) برای اعضا در هنگام ساخت تیم اضافه شده است.

### پاپ‌آپ افزودن عضو (`addMemberModal`)
*   این پاپ‌آپ برای افزودن سریع یک عضو به تیم (بدون باز کردن ویرایش کامل) استفاده می‌شود.
*   دراپ‌داون انتخاب نقش (Member/Admin) به آن اضافه شده است.

---

## ۲. فایل `Teams.aspx.cs` (بک‌اند)

منطق اصلی ذخیره‌سازی و بازیابی اطلاعات در این فایل قرار دارد.

### متد `rptTeams_ItemCommand` (بخش Edit)
زمانی که دکمه Edit زده می‌شود:
1.  اطلاعات تیم (نام و لوگو) بازیابی می‌شود.
2.  **مهم:** لیست اعضای تیم (`TeamMembers`) نیز از دیتابیس خوانده شده و به صورت JSON در فیلد مخفی `hfEditTeamMembersInitial` قرار می‌گیرد تا جاوااسکریپت بتواند لیست را پر کند.

### متد `btnSaveEdit_Click` (دکمه Team up در ویرایش)
این متد مسئول اعمال تمام تغییرات است:
1.  **بررسی نام تکراری:** چک می‌کند نام جدید با تیم دیگری تداخل نداشته باشد.
2.  **آپدیت اطلاعات پایه:** نام تیم و لوگو (در صورت آپلود) به‌روزرسانی می‌شوند. ستون `UpdatedAt` نیز آپدیت می‌شود.
3.  **مدیریت اعضا (Member Management):**
    *   لیست جدید اعضا را از JSON (`hfEditTeamMembers`) می‌خواند.
    *   **حذف:** اعضایی که در لیست جدید نیستند اما در دیتابیس بودند، حذف می‌شوند.
    *   **افزودن/ویرایش:** اعضای جدید اضافه می‌شوند و اگر عضوی از قبل بود، نقش (Role) او آپدیت می‌شود.

---

## ۳. فایل `assets/css/teams.css`

استایل‌های مربوط به مودال‌ها در این فایل تغییر کرده‌اند:
*   کلاس `.modal-content`: سایز و `max-width` تنظیم شده تا پاپ‌آپ‌ها جمع‌وجورتر باشند.
*   اضافه شدن اسکرول (`overflow-y: auto`) برای زمانی که تعداد اعضا زیاد می‌شود.

---

## ۴. راهنما برای تغییرات آینده

اگر می‌خواهید فیلد جدیدی به پاپ‌آپ‌ها اضافه کنید:

1.  **HTML:** فیلد ورودی را به `div` مربوطه در `Teams.aspx` اضافه کنید.
2.  **JavaScript:**
    *   اگر فیلد مربوط به عضو است، در تابع `addEditMemberRow` HTML آن را اضافه کنید.
    *   در تابع `prepareEditTeamData` مقدار آن را بگیرید و به آبجکت JSON اضافه کنید.
3.  **C# (`Teams.aspx.cs`):**
    *   کلاس `MemberDTO` را آپدیت کنید تا فیلد جدید را داشته باشد.
    *   در متد `btnSaveEdit_Click` یا `btnTeamUp_Click` مقدار جدید را در دیتابیس ذخیره کنید.
