# نسخه 7 - تغییرات و بهروزرسانیها

## تاریخ: 2024-12-07

### تغییرات کلی
در این نسخه، سیستم مدیریت کاربران بهبود یافته و صفحات جدیدی برای مدیریت پروژه، وظایف، تیمها و داشبورد اضافه شده است. مهمترین ویژگی جدید، سیستم انتخاب تصویر پروفایل پس از ورود یا ثبتنام کاربران است.

### صفحات جدید ایجاد شده

#### 1. Dashboard.aspx
- **مسیر**: `p:\TaskQuest\TaskQuest\TaskQuest\Dashboard.aspx`
- **توضیحات**: صفحه اصلی داشبورد کاربر پس از ورود موفق
- **ویژگیها**:
  - صفحه ساده با پیام خوشامدگویی "کاربر گرامی خوش آمدید"
  - طراحی مینیمال و تمیز
  - بررسی احراز هویت کاربر
  - فیکس مشکل انکودینگ برای نمایش صحیح متن فارسی

#### 2. Tasks.aspx
- **مسیر**: `p:\TaskQuest\TaskQuest\TaskQuest\Tasks.aspx`
- **توضیحات**: صفحه مدیریت وظایف و کارها
- **ویژگیها**:
  - لیست وظایف با امکان افزودن، ویرایش و حذف
  - پنجره مودال برای ایجاد وظیفه جدید
  - فیلتر و جستجو در وظایف
  - برچسبهای وضعیت برای هر وظیفه

#### 3. Projects.aspx
- **مسیر**: `p:\TaskQuest\TaskQuest\TaskQuest\Projects.aspx`
- **توضیحات**: صفحه مدیریت پروژهها
- **ویژگیها**:
  - نمایش لیست پروژهها
  - امکان ایجاد پروژه جدید
  - مدیریت وضعیت پروژهها
  - اختصاص پروژه به تیمها

#### 4. Teams.aspx
- **مسیر**: `p:\TaskQuest\TaskQuest\TaskQuest\Teams.aspx`
- **توضیحات**: صفحه مدیریت تیمها
- **ویژگیها**:
  - نمایش اعضای تیم
  - امکان افزودن اعضای جدید
  - مدیریت نقشها و دسترسیها
  - ارتباط با پروژهها

#### 5. ChooseAvatar.aspx
- **مسیر**: `p:\TaskQuest\TaskQuest\TaskQuest\ChooseAvatar.aspx`
- **توضیحات**: صفحه انتخاب تصویر پروفایل
- **ویژگیها**:
  - 6 تصویر پیشفرض برای انتخاب
  - امکان آپلود تصویر سفارشی
  - دکمه "انجام شد" برای ذخیره و ادامه
  - دکمه "فعلا از این مرحله بگذر" برای رد کردن این مرحله
  - ذخیره مسیر تصویر در پایگاه داده
  - آواتارهای کوچکتر (80px به جای 120px)
  - آواتارها در یک خط با قابلیت اسکرول افقی
  - طراحی واکنشگرا برای تمام سایزهای صفحه
  - فیکس مشکل انکودینگ برای نمایش صحیح متن فارسی

### تغییرات در صفحات موجود

#### Login.aspx.cs
- **تغییر**: تغییر مسیر پس از ورود موفق
- **قبل**: `Response.Redirect("Default.aspx")`
- **بعد**: `Response.Redirect("ChooseAvatar.aspx")`
- **دلیل**: هدایت کاربر به صفحه انتخاب تصویر پروفایل

#### SignUp.aspx.cs
- **تغییرات**:
  - اضافه شدن `using System.Web.Security;`
  - اضافه شدن خودکار ورود پس از ثبتنام موفق
  - تغییر مسیر پس از ثبتنام از `Login.aspx` به `ChooseAvatar.aspx`
- **کد جدید**:
```csharp
// Auto-login after successful signup
Session["User"] = username;
FormsAuthentication.SetAuthCookie(username, false);

// Redirect to avatar selection
Response.Redirect("ChooseAvatar.aspx");
```

### تغییرات پایگاه داده

#### جدول Users
- **ستون جدید**: `AvatarPath` (NVARCHAR(255))
- **روش اضافه شدن**: به صورت خودکار در زمان ذخیره تصویر پروفایل
- **اسکریپت بررسی و اضافه کردن**:
```sql
IF NOT EXISTS (SELECT * FROM sys.columns 
WHERE object_id = OBJECT_ID(N'Users') AND name = 'AvatarPath')
BEGIN
    ALTER TABLE Users ADD AvatarPath NVARCHAR(255)
END
```

### فایلهای تصویر جدید

#### تصاویر پیشفرض آواتار
- **مسیر**: `p:\TaskQuest\TaskQuest\TaskQuest\assets\images\avatars\`
- **فایلها**:
  - `avatar1.svg` - تصویر پیشفرض 1 (آبی)
  - `avatar2.svg` - تصویر پیشفرض 2 (سبز)
  - `avatar3.svg` - تصویر پیشفرض 3 (قرمز)
  - `avatar4.svg` - تصویر پیشفرض 4 (نارنجی)
  - `avatar5.svg` - تصویر پیشفرض 5 (بنفش)
  - `avatar6.svg` - تصویر پیشفرض 6 (صورتی)

#### تصویر آواتار پیشفرض
- **مسیر**: `assets/images/default-avatar.png`
- **استفاده**: زمانی که کاربر تصویری انتخاب نکرده باشد

### امنیت و بهبودها

#### امنیت
- استفاده از کوئریهای پارامتری برای جلوگیری از SQL Injection
- بررسی احراز هویت کاربر در تمام صفحات
- استفاده از Session برای مدیریت وضعیت کاربر

#### بهبودهای کد
- استفاده از `using` statement برای مدیریت منابع
- پیادهسازی متد `ShowError` برای نمایش پیامهای خطا
- استفاده از `WebConfigurationManager` برای دسترسی به رشته اتصال پایگاه داده
- پیادهسازی سیستم ارثبری از BasePage برای کنترل متمرکز
- حذف کدهای تکراری انکودینگ از صفحات
- اضافه کردن دستورات using لازم برای رفع خطاهای کامپایل

### پیادهسازی BasePage.cs

#### ویژگیهای کلاس BasePage
- **مسیر**: `p:\TaskQuest\TaskQuest\TaskQuest\BasePage.cs`
- **توضیحات**: کلاس پایه برای تمام صفحات ASP.NET جهت کنترل متمرکز انکودینگ
- **متدهای کلیدی**:
  - `OnInit()`: تنظیم کدگذاری پاسخ به UTF-8
  - `OnLoad()`: اعمال تنظیمات فرهنگ فارسی
  - `OnPreRender()`: اعتبارسنجی نهایی انکودینگ
  - `SetControlText()`: تنظیم متن کنترلهای Label به صورت ایمن
  - `SafeEncodePersianText()`: تبدیل بایتهای ناخوانا به متن فارسی صحیح

#### تنظیمات Web.config
```xml
<globalization fileEncoding="utf-8" requestEncoding="utf-8" responseEncoding="utf-8" culture="fa-IR" uiCulture="fa-IR" />
```

#### تغییرات در صفحات
- تبدیل متنهای استاتیک HTML به کنترلهای Label سروری
- استفاده از `SetControlText()` به جای تنظیم مستقیم متن
- حذف کدهای انکودینگ تکراری از code-behind صفحات

### مسیرهای مهم

#### صفحات ASPX
- `/Dashboard.aspx` - داشبورد اصلی
- `/Tasks.aspx` - مدیریت وظایف
- `/Projects.aspx` - مدیریت پروژهها
- `/Teams.aspx` - مدیریت تیمها
- `/ChooseAvatar.aspx` - انتخاب تصویر پروفایل

#### فایلهای CSS و JavaScript
- `/assets/css/style.css` - استایلهای اصلی
- `/assets/js/script.js` - اسکریپتهای جاواسکریپت

#### تصاویر
- `/assets/images/avatars/avatar[1-6].svg` - تصاویر پیشفرض آواتار
- `/assets/images/default-avatar.png` - تصویر پیشفرض عمومی

### نکات مهم

1. **تصویر پروفایل**: کاربران میتوانند از بین 6 تصویر پیشفرض انتخاب کنند یا تصویر خود را آپلود کنند
2. **رد کردن انتخاب**: کاربران میتوانند با زدن دکمه "فعلا از این مرحله بگذر" این مرحله را رد کنند
3. **ذخیره تصویر**: مسیر تصویر انتخاب شده در پایگاه داده ذخیره میشود
4. **نمایش تصویر**: تصویر پروفایل در تمام صفحات در قسمت اطلاعات کاربر نمایش داده میشود

### مشکلات برطرف شده

1. **مشکل**: کاربران پس از ورود به صفحه پیشفرض هدایت میشدند
   **راهحل**: تغییر مسیر به صفحه انتخاب تصویر پروفایل

2. **مشکل**: کاربران پس از ثبتنام باید دوباره وارد میشدند
   **راهحل**: اضافه شدن خودکار ورود پس از ثبتنام موفق

3. **مشکل**: نبود تصویر پروفایل برای کاربران
   **راهحل**: اضافه شدن سیستم انتخاب تصویر پروفایل با 6 تصویر پیشفرض

4. **مشکل**: صفحه داشبورد پیچیده و شلوغ بود
   **راهحل**: سادهسازی صفحه به پیام خوشامدگویی "کاربر گرامی خوش آمدید"

5. **مشکل**: انکودینگ نادرست در صفحات داشبورد و انتخاب آواتار
   **راهحل**: پیادهسازی سیستم انکودینگ متمرکز با استفاده از کلاس BasePage
   - ایجاد کلاس BasePage.cs برای کنترل متمرکز انکودینگ
   - تبدیل متنهای استاتیک به کنترلهای Label برای کنترل سروری
   - اضافه کردن متد SafeEncodePersianText() برای تبدیل بایت به رشته
   - تنظیمات globalization در Web.config برای UTF-8 و فرهنگ فارسی (fa-IR)
   - اضافه کردن استایل RTL به پیامهای خطا در جاواسکریپت

6. **مشکل**: آواتارهای بزرگ و چیدمان چندخطی
   **راهحل**: کوچک کردن آواتارها (80px) و چیدمان تکخطی با اسکرول افقی

### برنامههای آینده

- اضافه کردن سیستم آپلود تصویر سفارشی
- بهبود رابط کاربری صفحه انتخاب تصویر
- اضافه کردن امکان ویرایش تصویر پروفایل از داشبورد
- پیادهسازی سیستم اعلانها برای وظایف و پروژهها

### خطاهای کامپایل برطرف شده

#### CS0246 - BasePage not found
- **مشکل**: کلاس BasePage در صفحات شناخته نمیشد
- **راهحل**: اضافه کردن دستورات using لازم به صفحات:
```csharp
using System.Web;
using System.Web.UI;
```
- **راهحل نهایی**: بازیابی بستههای NuGet و اطمینان از در دسترس بودن کلاس BasePage

#### CS0103 - Session/Response/IsPostBack not found
- **مشکل**: متغیرها و متدهای ASP.NET در context صفحه در دسترس نبودند
- **راهحل**: اضافه کردن دستورات using برای فضای نام System.Web

#### صفحات بهروزرسانی شده
- Dashboard.aspx.cs - اضافه شدن دستورات using
- ChooseAvatar.aspx.cs - اضافه شدن دستورات using و حذف کدهای تکراری
- Tasks.aspx.cs - اضافه شدن دستورات using و حذف کدهای تکراری

#### رفع مشکلات کامپایل نهایی
- **مشکل**: خطاهای CS0246 همچنان پس از اضافه کردن دستورات using
- **راهحل**: بازیابی بستههای NuGet با استفاده از nuget.exe restore
- **نتایج**: تمام خطاهای CS0246 و CS0103 برطرف شدند و پروژه آماده کامپایل است